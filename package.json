{
  "name": "otsu-lib",
  "version": "1.202309.1",
  "description": "An unofficial module for a certain Rhythm Game",
  "main": "./dist/otsu.js",
  "typings": "./typings/otsu.d.ts",
  "scripts": {
    "clean": "rimraf dist && rimraf typings",
    "build": "npm run clean && tsc --build tsconfig.json",
    "test": "npm run build && node ./dist/otsu"
  },
  "repository": {
    "url": "https://github.com/seibaosu/otsu-lib"
  },
  "publishConfig": { "registry": "https://npm.pkg.github.com/" },
  "author": "Seiba",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "node-fetch": "^2.6.4"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.4",
    "rimraf": "^5.0.1"
  }
}
